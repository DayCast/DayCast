# ============================================
# DayCast 環境変数設定
# このファイルを .env.local にコピーして値を設定してください
# cp .env.local.example .env.local
# ============================================

# === AI Provider ===
# gemini: Gemini APIキーで直接接続（開発向け、簡単）
# vertexai: GCPサービスアカウントで接続（本番向け）
AI_PROVIDER=gemini

# AI Model設定
AI_MODEL=gemini-2.0-flash-exp
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=8192

# --- Gemini API（AI_PROVIDER=gemini の場合）---
# Google AI Studio で取得: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key

# --- Vertex AI（AI_PROVIDER=vertexai の場合）---
# GCPプロジェクトID
GCP_PROJECT_ID=your-gcp-project-id
# リージョン
GCP_LOCATION=asia-northeast1
# サービスアカウントJSONファイルのパス
# GCPコンソール > IAM > サービスアカウント > キーを作成(JSON)
GOOGLE_APPLICATION_CREDENTIALS=./keys/service-account.json

# === Weather Provider ===
# jma: 気象庁API（無料、APIキー不要、日本のみ）
# openweathermap: OpenWeatherMap（グローバル対応、APIキー必要）
WEATHER_PROVIDER=jma

# OpenWeatherMap APIキー（WEATHER_PROVIDER=openweathermap の場合のみ）
# https://openweathermap.org/api で取得
OPENWEATHERMAP_API_KEY=

# === Auth (NextAuth) ===
# openssl rand -base64 32 で生成
NEXTAUTH_SECRET=your-nextauth-secret
NEXTAUTH_URL=http://localhost:3000

# Google OAuth
# GCPコンソール > APIとサービス > 認証情報 > OAuth 2.0 クライアントID
AUTH_GOOGLE_ID=your-google-client-id
AUTH_GOOGLE_SECRET=your-google-client-secret

# === Database ===
DATABASE_URL=postgresql://user:password@localhost:5432/daycast

# === Cron ===
# daily-update APIエンドポイントの認証トークン
CRON_SECRET=your-cron-secret
